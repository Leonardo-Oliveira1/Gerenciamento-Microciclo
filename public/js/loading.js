function loading(main_container) {
    const loadingImg = document.getElementById("loading").style.display = "block";
    const OpacityMainContainer = document.getElementById(main_container).style.opacity = "0.2";
}
